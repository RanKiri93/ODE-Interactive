# Phase Plane Lab - Agent Handoff Notes

## מטרת הפרויקט

האפליקציה היא מעבדת מישור פאזה אינטראקטיבית לסטודנטים בקורס משוואות דיפרנציאליות רגילות. המטרה היא לאפשר לסטודנטים לראות, לבנות, ולחקור תמונות פאזה של מערכות ליניאריות דו־ממדיות

```math
X'=AX
```

כאשר `A` היא מטריצה ממשית `2x2`.

הגישה הכללית בפרויקט היא פדגוגית־ויזואלית: לא רק לחשב סיווג, אלא להציג את תמונת הפאזה, להסביר כיצד מציירים אותה, ולאפשר למשתמש לשלוט בדוגמאות ובנתונים.

## קבצים מרכזיים

- `app/page.tsx` - רוב הלוגיקה של האפליקציה: סיווג מטריצות, חישוב ערכים/וקטורים עצמיים, ציור Canvas, רכיבי UI, פעילות הרכבת המטריצה.
- `app/globals.css` - כל העיצוב המרכזי: מבנה המסכים, כרטיסים, Canvas, טפסים, מקרא, כרטיסי הסבר, תצוגת מטריצה מורכבת.
- `app/layout.tsx` - הגדרת layout, כיווניות RTL, ופונט Assistant.

## עקרונות עבודה עם המשתמש

- המשתמש מעדיף התקדמות הדרגתית ומבוקרת: להוסיף פעילות או סוג תמונה אחד, לקבל הערות, ואז להמשיך.
- חשוב מאוד לדייק בנוטציה מתמטית. אם יש ערבוב RTL/LTR, עדיף לפצל שורות או להשתמש בכרטיס/טבלה במקום משפט ארוך.
- המשתמש שם לב לפרטים ויזואליים קטנים: מיקום labels, גודל מטריצות, ריווח סביב נוסחאות, צבעים, ומה מופיע במקרא.
- לא להוסיף פיצ׳רים גדולים “בבת אחת” אם לא נתבקש. עדיף לשמור על שליטה ויזואלית ופדגוגית.
- עבור נוסחאות משתמשים ב־KaTeX דרך `MathText`, אבל אם KaTeX מתקשה בתצוגה מורכבת, מותר לעקוף עם HTML/CSS או SVG.

## מצב נוכחי של האפליקציה

האפליקציה הורחבה מאתר יחיד של מעבדת מישור פאזה לאתר־על קטן ללימוד מד״ר.

הנתיבים המרכזיים:

- `/` - עמוד בית של אתר הקורס, עם כרטיסים למודולי לימוד.
- `/phase-plane` - מודול מישור הפאזה.
- `/constant-coefficients-euler` - שלד ראשוני למודול “מקדמים קבועים ומשוואות אוילר”.

במודול מישור הפאזה יש כרגע שלושה טאבים:

- `מבוא` - טאב כניסה לימודי קצר, עם מקום עתידי לסרטון embedded.
- `מישור פאזה` - המעבדה הראשית.
- `הרכבת המטריצה` - פעילות שבה המשתמש בוחר סוג תמונה ומזין נתונים, והאפליקציה בונה מטריצה מתאימה.

הערה טכנית: כרגע `app/page.tsx` עדיין מכיל את רוב קוד המעבדה ומייצא את `PhasePlaneModule`, ו־`app/phase-plane/page.tsx` מציג אותו. זה נעשה כדי לבצע את שינוי המבנה בלי להזיז בבת אחת קובץ גדול מאוד. בהמשך, אם ממשיכים לפתח את האתר, כדאי לשקול חילוץ מסודר של המעבדה לקובץ רכיב ייעודי.

## המעבדה הראשית

במעבדה הראשית המשתמש מזין מטריצה `2x2`, מקבל סיווג, ערכים עצמיים, עקבה, דטרמיננטה ודיסקרימיננטה, ורואה תמונת פאזה.

סוגי התמונות שנתמכים:

- אוכף
- צומת יציב / לא יציב
- כוכב יציב / לא יציב
- צומת מנוון יציב / לא יציב
- מרכז
- ספירלה יציבה / לא יציבה
- ישר שיווי־משקל יציב / לא יציב
- מקרה נילפוטנטי
- שדה אפס

הציור אינו רק אינטגרציה נומרית כללית. עבור רוב הסוגים יש ציור אנליטי ייעודי:

- `drawSaddlePortrait`
- `drawNodePortrait`
- `drawStarPortrait`
- `drawDefectiveNodePortrait`
- `drawCenterPortrait`
- `drawSpiralPortrait`
- `drawZeroEigenPortrait`

כדאי לשמור על העיקרון הזה גם בהמשך.

## עקרונות ציור

- פתרונות שמתכנסים לראשית צריכים להגיע ויזואלית קרוב מאוד לראשית.
- חצים על מסלולים מחולקים לפי אורך מסך, לא לפי זמן, כדי לקבל צפיפות חזותית אחידה.
- עבור ערכים עצמיים ממשיים מציירים וקטורי רפרנס מקווקווים בצבע `#83AFF0`.
- באוכף יש הבחנה בצבעים בין הישרים העצמיים/רפרנס לבין המסלולים.
- בשדה אפס לא מציירים וקטור עצמי כלל. זה תוקן ב־`eigenVectorReferences`: עבור `zeroEigen.kind === "zero"` מחזירים `[]`.

## כרטיסי "אופן ציור התמונה"

בפאנל הניתוח של המעבדה הראשית מופיע כרטיס הסבר בהתאם לסוג התמונה.

כבר קיימים כרטיסים עבור:

- אוכף
- צומת
- כוכב
- צומת מנוון
- מרכז
- ספירלה
- מקרי ערך עצמי אפס

הסגנון הרצוי:

- כותרת: `אופן ציור התמונה`
- הסבר קצר בעברית.
- נוסחאות ב־KaTeX.
- טבלאות אסימפטוטיקה כאשר יש ערבוב של עברית ו־`t\to\infty`.
- להימנע ממשפטים ארוכים מדי עם הרבה MathText inline.

## פעילות "הרכבת המטריצה"

פעילות זו נמצאת בטאב נפרד.

המשתמש בוחר סוג תמונה, מזין ערכים עצמיים ווקטורים מתאימים, ומקבל מטריצה שמקיימת את הדרישות. בצד מוצגת תמונת הפאזה של המטריצה שנבנתה בעזרת אותו `PhaseCanvas` של המעבדה הראשית.

יש גם כפתור:

- `פתח במעבדה הראשית`

שמעביר את המטריצה שנבנתה לטאב המעבדה.

### מקרים ממשיים

נתמכים:

- אוכף
- צומת יציב / לא יציב
- כוכב יציב / לא יציב
- צומת מנוון יציב / לא יציב

במקרים לכסינים:

```math
A=PDP^{-1}
```

כאשר עמודות `P` הן הווקטורים העצמיים.

בכוכב:

```math
A=\lambda I
```

הווקטורים `V_1,V_2` משמשים בעיקר להדגשת העובדה שאפשר לבחור בסיס של וקטורים עצמיים, אבל המטריצה עצמה אינה תלויה בהם.

בצומת מנוון:

- מדגישים ערך עצמי `\lambda`.
- מבקשים וקטור עצמי `V_1`.
- מבקשים `וקטור חבר` `V_2`.
- הבנייה היא עם בלוק ז׳ורדן:

```math
J=\begin{pmatrix}\lambda&1\\0&\lambda\end{pmatrix},
\qquad A=PJP^{-1}
```

כאשר

```math
P=[\,V_1\ V_2\,]
```

ו־`V_2` אמור לקיים:

```math
(A-\lambda I)V_2=V_1
```

### מקרים מרוכבים

נתמכים:

- מרכז
- ספירלה יציבה
- ספירלה לא יציבה

במרכז המשתמש מזין מספר ממשי `\beta` עבור:

```math
\lambda=\beta i,\qquad \beta\ne0
```

בספירלה המשתמש מזין שני מספרים ממשיים:

```math
\lambda=\alpha+\beta i
```

כאשר:

- בספירלה יציבה: `\alpha<0`
- בספירלה לא יציבה: `\alpha>0`
- תמיד `\beta\ne0`

במקרים המרוכבים מספיק וקטור עצמי מרוכב אחד:

```math
V=U+iW
```

המשתמש מזין שני וקטורים ממשיים:

- `U` - חלק ממשי
- `W` - חלק מדומה

שני החלקים צריכים להיות לא אפס ובלתי־תלויים ליניארית.

הבנייה היא:

```math
P=[\,U\ W\,],
\qquad
B=\begin{pmatrix}\alpha&\beta\\-\beta&\alpha\end{pmatrix},
\qquad
A=PBP^{-1}
```

עבור מרכז לוקחים `\alpha=0`.

### UI של המקרים המרוכבים

עבור מרכז:

- החלונית העליונה כוללת את `\beta`.
- החלונית התחתונה כוללת את `U` ו־`W`.

עבור ספירלות:

- החלונית העליונה כוללת גם את `\alpha` וגם את `\beta`.
- החלונית התחתונה נקראת `הווקטור העצמי המרוכב`.
- החלונית התחתונה כוללת את `U` ו־`W`.

חשוב: המשתמש כבר העיר על מיקום `\beta\ne0`, ולכן יש לשמור אותו בחלונית העליונה בספירלות.

## תצוגת המטריצה בפעילות ההרכבה

הייתה בעיה משמעותית עם הצגת מטריצות כשיש שברים:

- `KaTeX` עם `pmatrix` ו־`\frac`/`\dfrac` גרם לתצוגה לא אחידה.
- CSS רגיל לא הגדיל את האיברים בצורה צפויה בגלל ירושה וספציפיות.

הפתרון הנוכחי:

- תצוגת המטריצה בכרטיס `המטריצה המתקבלת` מצוירת כ־`SVG`.
- `A=`, הסוגריים, המספרים, סימני מינוס וקווי השבר נשלטים ב־SVG/CSS.
- הפונקציות הרלוונטיות:
  - `rationalDisplayParts`
  - `SvgMatrixEntry`
  - `AssembledMatrixDisplay`

לא מומלץ לחזור ל־`pmatrix` רגיל עבור תצוגת המטריצה הזאת בלי לבדוק טוב ויזואלית.

## פונקציות מתמטיות חשובות

- `classify(matrix)` - סיווג המטריצה.
- `eigenSummary(matrix)` - טקסט לערכים עצמיים.
- `eigenVectorReferences(matrix)` - מחזיר וקטורי רפרנס לציור. חשוב: שדה אפס מחזיר `[]`.
- `diagonalMatrixFromEigenData`
- `defectiveMatrixFromJordanData`
- `complexMatrixFromEigenData`
- `matrixFromBasisModel`
- `inverseBasisMatrix`
- `basisMatrix`
- `multiplyMatrices`

## דברים שכדאי להיזהר מהם

- לא להציג `וקטורים עצמיים` במקרא אם אין וקטורי רפרנס בפועל.
- לא לצייר `V_1` בשדה אפס.
- במרכז ובספירלה, וקטורי `U,W` צריכים להיות בלתי־תלויים. אם הם תלויים, אין בסיס ממשי טוב.
- בצומת מנוון, `V_2` אינו וקטור עצמי אלא וקטור חבר. חשוב לא לכתוב עליו “וקטור עצמי”.
- בצומת רגיל, אם הערכים העצמיים שווים, זו אינה תמונת צומת רגיל אלא מקרה כוכב או מנוון.
- כאשר משנים טקסט עברי עם MathText, לבדוק RTL/LTR בתצוגה.

## בדיקות

בדיקת lint שרצה בהצלחה מספר פעמים:

```bash
npm.cmd run lint
```

אם מריצים ב־PowerShell, להשתמש ב־`npm.cmd` ולא `npm`, בגלל בעיות execution policy אפשריות.

## כיווני המשך אפשריים

- להוסיף פעילות נוספת תחת טאבים עתידיים.
- להוסיף דוגמאות/תרגילים מונחים לפעילות הרכבת המטריצה.
- להוסיף אפשרות לשמור או להעתיק מטריצה שנבנתה.
- לשפר עוד את תצוגת ה־SVG של המטריצה אם המשתמש מעיר על ריווח/גודל.
- להוסיף ולידציה פדגוגית יותר עם הודעות שמסבירות למה תנאי מסוים דרוש.
